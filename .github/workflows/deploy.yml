name: Deploy Story Service

on:
  push:
    branches:
      - prod
    paths:
      - "story-service/**"
      - ".github/workflows/deploy.yml"

env:
  SERVICE_NAME: story-service
  IMAGE_NAME: ${{ secrets.DOCKERHUB_USERNAME }}/story-service
  PORT: 3021

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Docker
        uses: docker/setup-buildx-action@v3

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        run: |
          cd story-service
          docker build -t $IMAGE_NAME:${{ github.sha }} .
          docker push $IMAGE_NAME:${{ github.sha }}
          docker tag $IMAGE_NAME:${{ github.sha }} $IMAGE_NAME:latest
          docker push $IMAGE_NAME:latest

      # Descomentar y adaptar según el método de deploy (ECS, EC2, etc.)
      # - name: Deploy to AWS ECS/EC2
      #   run: |
      #     echo "Aquí se haría el deploy a AWS ECS/EC2, usando la imagen: $IMAGE_NAME:${{ github.sha }}"
