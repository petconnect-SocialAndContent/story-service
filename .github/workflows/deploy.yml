name: Deploy Story Service

on:
  push:
    paths:
      - "story-service/**"
    branches:
      - main

env:
  SERVICE_NAME: story-service
  IMAGE_NAME: ${{ secrets.DOCKERHUB_USERNAME }}/story-service

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repo
        uses: actions/checkout@v3

      - name: 🐳 Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 🔐 Log in to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: 🛠️ Build and push image
        uses: docker/build-push-action@v5
        with:
          context: ./story-service
          file: ./story-service/Dockerfile
          push: true
          tags: ${{ env.IMAGE_NAME }}:latest

  # Optional: Deploy to AWS
  # deploy:
  #   needs: build-and-push
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: 🚀 Deploy to ECS / EC2 / Lambda
  #       run: echo "Aquí iría el despliegue en AWS si lo necesitas"
